# Bajaj Finserv Health Qualifier 1

Python solution for webhook generation and SQL query submission.

## Steps

1. Generates webhook using provided API
2. Receives access token
3. Solves assigned SQL problem
4. Sends final SQL query to webhook endpoint

## Run

```bash
pip install -r requirements.txt
python main.py# Bajaj-finserv-qualifier
